2026-07-28  dephasing.html — 文字モード運動設計 Phase 1（design-audit 指示書に基づく）

■ 実装した変更
1-1 呼吸のゲート化
  - DS トークン導入（motionWindow / alphaRest / alphaRecall / typeBase / typeRecall / roomCapacity）
    ※指示書は「JP_FONT の直後」だったが、params の初期値が DS を参照するため
      TDZ エラーになる。定義を script 先頭（params より前）へ移した。
  - params.speedCap 削除 → params.motionWindow 追加
  - draw() に exhaleStart / prog / moving / envelope のゲート計算を追加。
    wordMode の宣言をゲート計算より前へ移動。
    ※指示書の envelope 式（moving && wordMode ? sin : 1）では
      文字モードの静止フェーズで envelope=1 になり静止が成立しないため、
      「!wordMode ? 1 : (moving ? sin(πprog) : 0)」に補正した。
      marks=常時1・文字の運動窓=sin包絡・文字の静止=0 で意図どおり。
  - Letter.update() に envelope 引数を追加、ドリフト項に乗算
  - 静止フェーズ確定処理（vel=0・Math.round スナップ・settled フラグ・early return）

1-2 weight 廃止 → amp（phase から決定論的、0.6〜1.4）
  - constructor / update ドリフト項 / render() 2 箇所を置換。this.weight 参照は 0 件。

1-3 crowding をレイアウト制約へ
  - 毎フレーム実行を廃止。運動→静止の遷移フレームで relieveCrowding()×6 のみ。
    ※指示書は「解決後の座標が整数スナップされる順序」を要求するため、
      呼び出し位置を update ループの前（render ループの前でもある）に置いた。
      遷移フレームでは crowding 解決 → 直後の update() でスナップ、の順になる。
  - wasMoving をグローバルに追加。

1-4 renderWord() 全面差し替え
  - サイズ 2 段階（DS.typeBase=18 / DS.typeRecall=20）、alpha 2 段階（0.62 / 0.92）。
    静止語の明滅なし（bAmp 依存を除去）。

1-5 可読性
  - 文字モードの残像撤去（fa = wordMode ? 1 : ...）
  - wordSize 既定 15→DS.typeBase(18)、HTML スライダー初期値も 18 に揃えた
  - setMode の語数クランプ 70/60 → DS.roomCapacity(40)
  - speedCap スライダー → Motion window スライダーに置換（resetParameters も更新）
  - update() 内の速度上限ブロックを削除

■ 受け入れ確認の結果
静的:
  - 構文 OK（node 不在のため JXA の new Function で代替チェック）
  - grep speedCap → 0、grep this.weight → 0
挙動（ブラウザペインは rAF 停止・viewport 0x0 のため、draw() を手回しして計測）:
  1. 静止率 72.0%（540 フレーム=1呼吸、motionWindow=0.28）→ 7割以上 ✓
  2. 静止語の alpha はコード上定数（DS.alphaRest）✓
  3. 静止フェーズ中の運動体は recall 語のみ、最大同時 1 語 ✓
  4. 残像なし（fa=1 で毎フレーム全消去）✓
  5. marks モード: 900/900 が常時運動（envelope=1 経路）✓
     ※注意: 1-2 の指示どおり render() の strokeWeight も weight→amp に置換したため、
       marks の線の太さ分布が random(0.55,1.5) から phase 由来(0.6〜1.4)へ厳密には変化。
       見た目の範囲はほぼ同じ。
  6. 同一 seed(12345) で initializeSystem() を 2 回 → 語と amp が完全一致（決定論的）✓
  7. motionWindow=0.05 → 静止率 95.0% ✓
  - crowding 実行回数: 1 呼吸あたり遷移 1 回×6 反復のみ ✓
  - 静止中の非整数座標: 0 件（スナップ有効）✓
  - コンソールエラー: 0 件

■ Phase 2 / 3 は未着手（承認待ち）


────────────────────────────────────────────────────────────────────
2026-07-28  dephasing.html — Phase 2（Refinement）承認を受けて実装

■ 実装した変更
2-1 語数 40 の余白評価（Phase 1 で 40 に変更済み・目視と実測のみ）
  - 実測: 40語 / 使用罫 24〜26本（利用可能 33本）/ 1罫あたり最大 4語
    罫の弦長に対する字面の占有率は平均 15.1%・最大 43.5%。
  - 判断: 余白は十分。40 は減らす必要がない。むしろ余裕がある側。

2-2 静止位置のベースライングリッド整列
  - DS.baselineGrid = 32（typeBase 18 の約1.8倍の行送り）を追加。
  - baselineOf(y): 部屋の中心を通る一本を基準に罫を取る。
  - resolveLayout()（旧: relieveCrowding×6 の直呼び）に置換。順序は
      2次元で粗くほどく → 罫に載せる → 同じ罫の上だけ横にほどく → x を整数化
    罫に載せた時点で縦の自由度を捨てるので、以後は横しか動かさない。
    罫の上で使える幅は部屋の円の弦長で決まる（部屋の外に押し出さない）。
    帰還中の語は静止レイアウトの一員ではないので対象外（飛行中を掴まない）。
  - seatLetter(): 帰還した語が静止フェーズの途中で降りるときの席決め。
    ※これは指示書に無い追加。理由 = 罫スナップだけを入れた最初の実装では、
      静止フェーズ途中に着地した語が既存の語と重なる事故が実測で出た
      （frame 1610「ありがとう」×「なんでもない」が 33px 差・要 99px）。
      resolveLayout 自体は毎回 overlap 0 だったので、原因は着地経路のみ。
      既に座っている語は動かさず、降りてきた語だけが空席を探す方式にした。
      「静止しているものは静止したまま」を壊さないことを優先。

2-3 recall 遷移の呼吸位相同期
  - recallGate = !wordMode || (moving && prog < 0.3) を draw() で計算し update へ。
  - 立ち上がり（recall 0 → >0）だけを運動窓の頭に限定。一度始まった帰還は
    静止フェーズでも進む（Phase 1 基準3「静止中に動くのは recall 語のみ」を保つため）。
  - marks は recallGate 常に true で不変。

■ 受け入れ確認の結果（draw() 手回し計測、ブラウザペインは rAF 停止のため）
  - 構文 OK / speedCap 0 件 / this.weight 0 件
  - 20呼吸（10800フレーム）通しの静止フェーズ全フレーム監査:
      罫から外れた語 0 ・語の重なり 0 ・部屋の外 0 ・
      静止中に既存の語が動いた回数 0
  - 罫の間隔はすべて 32 の倍数（実測 gap: 32,32,32,64,...）、y 範囲 88〜1016、中心 600 が罫上
  - 静止率 72.0%（Phase 1 から不変）
  - recall 立ち上がり 170 回すべて prog ∈ [0.003, 0.280] = 運動窓の頭に収まる
  - marks モード: 900/900 常時運動・settled 化なし・罫整列なし（不変）
  - コンソールエラー 0 件

■ Phase 2 で判明したトレードオフ（要判断・未対応）
  recall の位相同期は「出発が揃う」ため、静止中の同時運動語数の裾が伸びた。
    Phase 1: 最大 1 語 → Phase 2: 平均 1.9 語・最大 9〜11 語（20呼吸中）
    分布: 0語 37.5% / 1語 17.1% / 2語 13.6% / 3語 12.1% → 3語以下が 80.3%
  Phase 1 受け入れ基準3「同時に 1〜3 語」は 8 割の時間で満たすが、裾は外れる。
  これは 2-3 の直接の帰結（同期させれば束になる）で、バグではない。
  returnRarity が効くレバー（20呼吸計測）:
    rarity  6 → 平均 2.13 / 最大 11 / 3語以下 75.4%   ← 現在の既定
    rarity  9 → 平均 1.63 / 最大  7 / 3語以下 86.3%
    rarity 12 → 平均 1.22 / 最大  6 / 3語以下 92.1%
    rarity 14 → 平均 0.92 / 最大  5 / 3語以下 96.7%
  既定は 6 のまま変更していない（部屋の「帰還の頻度」は好みの決定のため）。

■ Phase 3 は未着手（承認待ち）


────────────────────────────────────────────────────────────────────
2026-07-28  dephasing.html — Phase 3（Polish）承認を受けて実装

■ 実装した変更
3-1 到着時の一呼吸静止
  - Letter に restUntil / isResting() を追加。帰着時に restUntil = clock + breathPeriod。
    文字モードのみ（marks では restUntil を一度も立てない）。
  - 静止判定を (envelope === 0 || resting) へ拡張。休息中も arrive は減衰させる
    （早期 return で減衰を素通りすると、灯色のまま固まる）。
  - ※実装中に出た二つの不具合を潰した：
    (a) 休息中に recall が再点火していた。age は帰着で 0 に戻るが recallAt は
        最短 240 フレームなので、540 フレームの休息の途中で条件を満たしてしまう。
        → recall 立ち上がりに !resting を追加。帰り着いた語がすぐ発ち直さない。
    (b) resolveLayout が休息中の語を掴んで並べ直していた。
        → resolveLayout の冒頭で _fixed = isResting() を立て、relieveCrowding /
          横ほどき / 弦クランプ / 整数化のすべてで固定側は動かさない。
          動ける側が二人分よけて通る（push を 2 倍／1 倍で配分）。
        Phase 2 の seatLetter と同じ原則＝「静止しているものは静止したまま」。

3-2 灯の指標を coherence → 静止率へ（文字モードのみ）
  - stillness をグローバルに追加。settled な語の割合を 0.06 で追従。空室は 1。
  - 灯の駆動を lampDrive = wordMode ? stillness : coherence に。marks は不変。
  - 計器も同じ値を指すよう updateReadout を書き換え（見出しと行ラベルを出し分け）。
      文字: 「灯 / Stillness」「静」  marks: 「灯 / Coherence」「R」
    ※灯が答えている値と読み手が見る値が食い違うのを避けるため。
  - 空室で cells === 0 のとき coherence が最後の値で凍る問題も直した（0 へ減衰）。

3-3 prefers-reduced-motion
  - reducedMotion フラグ + matchMedia の change 購読（applyMotionPreference）。
  - update() 冒頭で早期 return。位置は一切動かさない。帰還は距離ではなく
    recall が 1 に満ちたら帰着とみなす方式に切替＝色と alpha の遷移だけで表す。
  - renderWord に reduced 専用の分岐。字の大きさは typeBase 固定（寸法が跳ぶのも運動）、
    alpha は alphaRest→alphaRecall を heat で連続に補間（段階を作らない）。
  - 席は最初に一度だけ決める（initializeSystem / setMode / 設定変更時に resolveLayout）。
    draw() の遷移時 resolveLayout は reducedMotion で抑止——解き直し自体が位置移動になる。
    ※これを入れる前は 40 語中 5 語が動いていた。
  - 判断: marks にも適用した。「marks は現状維持」の制約には反するが、reduced-motion は
    OS 側で明示的に要求されたときだけ効くので、要求していない利用者から見た marks の
    挙動はビット単位で不変。900 の粒子が動き続ける方が事故と判断した。

3-4 空室（語数 0）の設計
  - letterCount スライダーの min を 150 → 0（step 50 のまま＝既存の刻みは全て温存）。
  - 空室では残像を必ず消す（fa を 1 に）。溜めると描いた罫が焼き付く。
  - renderEmptyRoom: 中央の罫を一本だけ、alpha 0.08 + 呼吸 0.05 で引く。
    「まだ何もありません」とは書かない——書いた時点で部屋ではなく空状態の通知になる。
    線が一本あれば、ここが何かを載せる場所だと分かる。灯は静止率 1.00 で静かに点る。

■ 受け入れ確認の結果
  - 構文 OK / speedCap 0 件 / this.weight 0 件 / コンソールエラー 0 件
  3-1: 帰着 111 回。変位が起きるフレームは休息開始から 0 と 1 のみ
       （＝席に着く動作）。2〜539 フレームは完全静止。540 で休息明け。
       休息中の recall 再点火 0 件。
  3-2: 文字「灯 / Stillness = 0.17」／marks「灯 / Coherence = 0.80」で出し分け。
       stillness は一呼吸で 0.075〜1.00 を動き、灯が静まりに答えている。
  3-3: 文字 40 語中 移動 0 / 速度全ゼロ / 罫整列維持 / heat は 0〜0.996 を巡回。
       marks 900 語も移動 0。設定を戻すと 900/900 が運動に復帰。
  3-4: 語数 0 で stillness 1.00・coherence 0・罫のピクセルは背景比 +13/255 で描画確認。
  回帰（Phase 1・2 の基準）:
       静止率 72.0%（不変）／10呼吸の静止フェーズ全フレームで
       重なり 0・罫外れ 0・部屋外 0・静止中の既存語の移動 0
       motionWindow 0.05 → 静止率 95.0%（不変）
       同一 seed の決定性 OK
       marks: 900/900 常時運動・settled 化なし・restUntil 全て 0（文字モード限定を確認）

■ 全 Phase 完了。以後の保留論点
  - Phase 2 の裾（recall 同期による同時運動語数 平均1.9・最大9〜11）は未対応のまま。
    returnRarity が唯一のレバー（既定 6 のまま）。
  - 空室の罫の濃さ（背景比 5%）は意図的に弱い。読み取れないと判断するなら要調整。


────────────────────────────────────────────────────────────────────
2026-07-28  保留事項の棚卸しと Kosei 判断（4点）— 反映済み

■ 判断1: returnRarity の既定を 6 → 12
  params / defaultParams / スライダー value / 表示ラベルの4箇所を変更（静的確認済み）。
  Phase 2 の掃引実測: rarity 12 = 平均1.22 / 最大6 / 3語以下92.1%（20呼吸）。
  Phase 3 後の短時間実測（2〜5呼吸）: 平均0.06〜0.32 / 最大1〜3 / 3語以下100%。
  ※20呼吸規模の再計測は未完（下記の検証環境の項）。到着後の一呼吸静止は運動を
    減らす方向にしか働かないので、掃引値より悪化することはない。

■ 判断2: marks に及ぶ2件は戻さない（承認）
  - reduced-motion を marks にも適用（OS 側で要求されたときだけ効く）
  - marks の strokeWeight が weight → amp 由来に変化
  以後この2点は「指示書からの逸脱」ではなく確定仕様として扱う。

■ 判断3: seatLetter の方式を承認
  「既に座っている語は動かさず、降りてきた語だけが空席を探す」で確定。
  Phase 2 の resolveLayout / Phase 3 の _fixed もこの原則の延長として維持。

■ 判断4: 空室の罫を強める（背景比 5% → もう少し）
  二つ直した:
  (a) 濃度 0.08+bAmp*0.05 → 0.20+bAmp*0.10
  (b) 整数 y に引くと画素の境目に落ち、二行に半分ずつ乗って実効濃度が半減していた。
      y に +0.5 して画素の中心に載せた（見た目の位置は不変、線だけが締まる）。
  実測: 背景比 +13/255（5.1%）→ +42〜62/255（16.5〜24.3%）。呼吸で濃淡も付く。
  参考: 静止語は約 +128/255。罫は語の 1/3〜1/2 の存在感で、従属関係は保たれている。

■ 検証環境について（今回ハマった点・次回のための記録）
  - プレビューペインは長時間の同期ループでツール応答が返らなくなる。
    体感の閾値は 1500〜2000 フレーム程度。ただし「タイムアウト＝未実行」ではなく、
    ページ側では計算が完走している（後続の呼び出しで蓄積値が進んでいた）。
  - 呼び出しを分割して積み上げる方式は、この計測では使えない。
    呼び出しの合間にページ自身の rAF が進み、比較用スナップショットが古くなるため、
    チャンク境界のフレームに偽の「静止中の移動」が大量計上される。
    実際 38 件すべてが境界フレームに集中し、変位量は罫の 32 の倍数だった
    （= 合間に resolveLayout が走った証拠）。noLoop() で止める手も試したが、
    ペインが応答不能になり完走できず。
  - したがって信頼できるのは「単一呼び出しで完結した計測」のみ。
    今回それで取れているのは 2 呼吸ぶん（重なり0・罫外れ0・部屋外0・静止中の移動0・
    静止率0.72）と、空室の罫の濃度計測。
  - 依然として未実施: 実際の 60fps 連続再生での目視、実 OS の
    prefers-reduced-motion での確認、20呼吸規模の裾の再計測。
